# Vue client → ASP.NET Core API (must include /api)
# Local:
VITE_API_BASE_URL=http://localhost:5071/api
# Render (Static Site env + rebuild):
# VITE_API_BASE_URL=https://ims-api-ftzr.onrender.com/api
